# Pre-Launch TODO

## Email
- [ ] Change email FROM address in `src/lib/email.ts` from `onboarding@resend.dev` to the real domain (e.g. `bookings@yourdomain.com`). Currently using Resend's test domain for development since the real domain isn't included in the open-source repo.
